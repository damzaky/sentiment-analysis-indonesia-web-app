# sentiment-analysis-indonesia-demo

## Installing
`npm install -g browserify`

`npm install`

## Building
### Bundling package for browser
`browserify module_exporter.js --standalone browserifyBundle > ./bundle.js`

### Train the model
`node trainer.js`